// Disabled by default. The lab deployment mounts an environment-specific file
// at this path. Production remains disabled until its deployment is approved.
window.froysteinAnalytics = Object.freeze({ enabled: false });
